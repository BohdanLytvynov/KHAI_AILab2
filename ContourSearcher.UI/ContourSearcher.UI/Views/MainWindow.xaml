<Window x:Class="ContourSearcher.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ContourSearcher.UI"
        xmlns:vm="clr-namespace:ContourSearcher.UI.ViewModels"
        xmlns:txtBxHelper="clr-namespace:ContourSearcher.UI.Helpers"
        mc:Ignorable="d"
        Title="{Binding Title, UpdateSourceTrigger=PropertyChanged}" 
        Height="650"
        Width="800"
        d:DataContext="{d:DesignInstance vm:MainWindowViewModel, IsDesignTimeCreatable=True}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <ToggleButton IsChecked="{Binding LoadImageEnabled, UpdateSourceTrigger=PropertyChanged}"
                Command="{Binding OnLoadImageTabButtonPressed}"
                Content="Load Image" 
                          Style="{StaticResource SimpleTogleButton}" Margin="10,0,0,0"/>
            <ToggleButton Style="{StaticResource SimpleTogleButton}"
                IsChecked="{Binding ImageProcessingEnabled, UpdateSourceTrigger=PropertyChanged}" Command="{Binding OnImageProcessTabButton}"
                Content="Process Image" Margin="10,0,0,0"/>
            <ToggleButton Style="{StaticResource SimpleTogleButton}"
                Command="{Binding OnSearchContoursTabPressed}"
                          IsChecked="{Binding ContourSearcherEnabled, 
                UpdateSourceTrigger=PropertyChanged}"
                Content="Find Contours" Margin="10,0,0,0"/>
        </StackPanel>
        <Frame x:Name="Main" Grid.Row="1" Margin="0,10,0,0"
               Content="{Binding Frame, 
            UpdateSourceTrigger=PropertyChanged}"
               NavigationUIVisibility="Hidden">
        </Frame>
        <Grid Grid.Row="1" x:Name="OptionsGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="87*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="80*"/>
            </Grid.ColumnDefinitions>
            <GridSplitter Grid.Column="1" Style="{StaticResource VerticalGridSplitterStyle}"/>
            <Frame x:Name="Right" Grid.Column="2"
                   Content="{Binding RightFrame, 
                UpdateSourceTrigger=PropertyChanged}"
                   NavigationUIVisibility="Hidden">
            </Frame>
        </Grid>
    </Grid>
</Window>
