<UserControl x:Class="UserControls.ImageViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserControls"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <system:Double x:Key="FontSize">28
        </system:Double>

        <Style x:Key="FontSizes">
            <Style.Setters>
                <Setter Property="ItemsControl.FontSize" Value="{StaticResource FontSize}"/>
            </Style.Setters>
        </Style>
        
        <Style x:Key="Margins">
            <Style.Setters>
                <Setter Property="ItemsControl.Margin" Value="20"/>
            </Style.Setters>
        </Style>


        <Style x:Key="Borders" BasedOn="{StaticResource Margins}" TargetType="Border">
            <Style.Setters>
                <Setter Property="BorderBrush" Value="Black"/>
                <Setter Property="BorderThickness" Value="3"/>
                <Setter Property="CornerRadius" Value="10"/>
            </Style.Setters>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="Container">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="7"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>
        <Border x:Name="ImageBorder"
                Style="{StaticResource Borders}">
            <Image x:Name="Img" Grid.Row="0"/>
        </Border>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="10"/>
            </Grid.ColumnDefinitions>
            <Button Content="Open" Grid.Column="1" x:Name="OpenButton"
                    Click="OpenButton_Click" Style="{StaticResource FontSizes}"/>
            <TextBlock Grid.Column="3" Background="White" 
                       x:Name="PathTextBox"                       
                       Text="Path will be here..."
                       Style="{StaticResource FontSizes}"
                       />
        </Grid>
    </Grid>
</UserControl>
